# ZomboidControl — project conventions

The conventions live in **[CLAUDE.md](CLAUDE.md)**. Read that file.

This one is a pointer rather than a copy: it was a copy, it fell four
sections and two hundred lines behind, and nobody noticed until the two
were compared. Two files stating the same rules drift, and the stale one
is then worse than no file at all.

Before starting any task, read **[CONTEXT.md](CONTEXT.md)** — the record
of what is built, what is verified, what is broken, and what the next
step is.
